import type { TokenSearchResponse, TokenTagResponse, TokenCategoryResponse, TokenRecentResponse, TokenTag, TokenCategory, TimeInterval } from '../types';
export interface TokenSearchOptions {
    /**
     * Search for a token and its information by its symbol, name or mint address
     * Comma-separate to ONLY search for multiple mint addresses
     * Limit to 100 mint addresses in query
     * Default to 20 mints in response when searching via symbol or name
     */
    query: string;
}
export interface TokenTagOptions {
    /**
     * Tag to filter tokens by
     * Possible values: 'lst' | 'verified'
     */
    query: TokenTag;
}
export interface TokenCategoryOptions {
    /**
     * Category of tokens to retrieve
     * Possible values: 'toporganicscore' | 'toptraded' | 'toptrending'
     */
    category: TokenCategory;
    /**
     * Time interval for more accuracy
     * Possible values: '5m' | '1h' | '6h' | '24h'
     */
    interval: TimeInterval;
    /**
     * Maximum number of results to return (default 50, max 100)
     */
    limit?: number;
}
export declare class TokensAPI {
    private baseUrl;
    constructor(baseUrl?: string);
    /**
     * Search for tokens by symbol, name, or mint address
     * @param options - Search options
     * @returns Array of tokens matching the search criteria
     */
    search(options: TokenSearchOptions): Promise<TokenSearchResponse>;
    /**
     * Get tokens by tag
     * @param options - Tag options
     * @returns Array of all tokens belonging to the tag
     */
    getByTag(options: TokenTagOptions): Promise<TokenTagResponse>;
    /**
     * Get tokens by category and interval
     * @param options - Category options
     * @returns Array of tokens for the given category and interval
     */
    getByCategory(options: TokenCategoryOptions): Promise<TokenCategoryResponse>;
    /**
     * Get recently created tokens
     * @returns Array of tokens that recently had their first created pool (default 30 tokens)
     */
    getRecent(): Promise<TokenRecentResponse>;
}
//# sourceMappingURL=tokens.d.ts.map